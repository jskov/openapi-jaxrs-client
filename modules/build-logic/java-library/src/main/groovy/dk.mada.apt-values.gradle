plugins {
    id('java')
    id('dk.mada.java')
}

configurations {
    aptValue
}

dependencies {
    aptValue            (platform('dk.mada.jaxrs:openapi-jaxrs-parent'))

    annotationProcessor "org.immutables:value"
    aptValue            "org.immutables:value"
    compileOnly         "org.immutables:value::annotations"
}
