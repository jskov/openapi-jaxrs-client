plugins {
    id 'com.diffplug.spotless'
}

spotless {
    java {
        target 'src/main/java/**/*.java'
        eclipse().configFile(rootProject.file("src/config/spotless/eclipse-formatter-mada.xml"))
        formatAnnotations()
    }
}
