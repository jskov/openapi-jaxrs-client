plugins {
    id('java')

    id 'dk.mada.sonarqube'
    id 'dk.mada.spotless'
// desired:    id 'dk.mada.apt-errorprone'
}

group "${groupId}"

java {
    toolchain {
        languageVersion.set(JavaLanguageVersion.of(17))
    }
}

dependencies {
    implementation      (platform('dk.mada.jaxrs:openapi-jaxrs-bom'))
    annotationProcessor (platform('dk.mada.jaxrs:openapi-jaxrs-bom'))

    testImplementation  (platform('dk.mada.jaxrs:test-platform'))
    testImplementation  'org.junit.jupiter:junit-jupiter'
    testImplementation  'org.junit.jupiter:junit-jupiter-params'
    testImplementation  'org.junit.jupiter:junit-jupiter-engine'
    testImplementation  'org.junit.jupiter:junit-jupiter-api'
    testImplementation  'org.junit.platform:junit-platform-runner'

    testImplementation  "org.assertj:assertj-core"
}

tasks.named("test") {
    useJUnitPlatform()
}
