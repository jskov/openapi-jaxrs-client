plugins {
    id('java')
}

group "${groupId}"

java {
    toolchain {
        languageVersion.set(JavaLanguageVersion.of(17))
    }
}


dependencies {
    implementation      (platform('dk.mada.platform:product-platform'))
    annotationProcessor (platform('dk.mada.platform:product-platform'))

    testImplementation(platform('dk.mada.platform:test-platform'))
    testImplementation('org.junit.jupiter:junit-jupiter')
}

tasks.named("test") {
    useJUnitPlatform()
}
