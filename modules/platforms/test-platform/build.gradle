plugins {
    id('java-platform')
}

group = 'dk.mada.jaxrs'

// allow the definition of dependencies to other platforms like the JUnit 5 BOM
javaPlatform.allowDependencies()

dependencies {
    api   (platform('org.junit:junit-bom:5.9.2'))
    api   (platform('org.assertj:assertj-core:3.24.2'))
}
