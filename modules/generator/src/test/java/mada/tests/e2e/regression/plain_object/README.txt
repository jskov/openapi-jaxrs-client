Manually created openapi.yaml from aftalepakke for #427

Contains plain object DTO:

"TilknytAftalegrundlagResponse": {
      "type": "object",
      "properties": {}
    },

which was not rendered (because Object) but filtered out (because no properties).
