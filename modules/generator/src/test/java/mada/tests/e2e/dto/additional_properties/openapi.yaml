---
openapi: 3.0.3
info:
  title: openapi-examples API
  version: 1.0.0-SNAPSHOT
paths:
  /dtos/additional_properties:
    get:
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Event.read'
  # From BD, generated for API 3.0.4
  /kkt-income/incomes/{ref}/{label}:
    patch:
      operationId: patchIncome
      parameters:
        - required: true
          name: label
          in: path
          schema:
            type: string
        - required: true
          name: ref
          in: path
          schema:
            format: int32
            type: integer
      requestBody:
        description: Only 'description' and 'isWorkingDraft' are allowed to be patched
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: {}
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Event.read'
components:
  schemas:
    # from https://stoplight.io/api/v1/projects/teamup/api/nodes/reference/generated_docs_public.yaml
    Event.read:
      title: Read Event
      description: Content of an event when fetching a single event
      type: object
      properties:
        custom:
          title: Custom field values for user-defined custom event fields.
          description: This is an object with custom field ids as keys and values as strings or arrays of strings for fields of type "choice".
          type: object
          example:
            field1: value
            choicefield:
              - a
              - b
          nullable: true
          additionalProperties: true
    # From https://github.com/jskov/openapi-jaxrs-client/issues/817
    # FIXME: need better rendering with extends
    MultivaluedHashMapStringString:
      type: object
      additionalProperties:
        type: array
        items:
          type: string
    
          