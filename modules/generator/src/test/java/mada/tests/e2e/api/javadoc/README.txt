Make sure non-code information is represented in javadoc.

TODO: also make sure the (presently missing) parameter data are used

Manually appended example seen (from .Net?) with weird newline handling.
