plugins {
    id 'eclipse'

    id 'com.diffplug.eclipse.apt' version '4.4.1'
    id 'dk.mada.reproducible' version '1.0.2'
    id 'dk.mada.style' version '1.20'
}

apply from: "${project.rootDir}/gradle/dk.mada.jacoco-report.gradle"
apply from: "${project.rootDir}/gradle/dk.mada.docs.gradle"
