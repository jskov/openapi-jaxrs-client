Note that the openapi.yaml is part generated, and part canned (openapi-external.yaml).

The canned part represents input from generators that make DTOs for OpenApi basic types.
These should (depending on parser options) be treated as basic types and not stand-alone DTOs.
