Note that the openapi.yaml is part generated, and part canned (openapi-external.yaml).

The canned part represents input from generators that make DTOs for OpenApi lists.
These should (depending on parser options) be treated as List types and not stand-alone DTOs.
